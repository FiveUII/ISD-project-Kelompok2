---
phase: 03-loans-circulation
plan: "03"
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - frontend/src/pages/MyLoansPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [LOAN-03, LOAN-04]

must_haves:
  truths:
    - "Student at /my-loans sees a card list of all their active (non-returned) loans"
    - "Each loan card shows: Book title + author, Due date formatted as a readable date, 'Overdue' red badge when is_overdue=true"
    - "Empty state shows 'No active loans. Browse the catalog to find a book.' with a link to /catalog"
    - "Student NavBar 'My Loans' link (added in Plan 02) routes to this page"
    - "/my-loans is a student-only route — librarian accessing it is redirected to /catalog by AppLayout"
  artifacts:
    - path: "frontend/src/pages/MyLoansPage.tsx"
      provides: "Student My Loans page with card list of active loans"
      contains: "MyLoansPage"
    - path: "frontend/src/App.tsx"
      provides: "Route for /my-loans added inside authenticated AppLayout"
      contains: "/my-loans"
  key_links:
    - from: "frontend/src/pages/MyLoansPage.tsx"
      to: "GET /api/loans/my"
      via: "useQuery(['my-loans']) → getMyLoans()"
      pattern: "my-loans"
---

<objective>
## Phase Goal

**As a** student, **I want to** see all my currently borrowed books with due dates, **so that** I know what I have checked out and can avoid overdue fines.

Student My Loans page: a card-based view of all active loans for the logged-in student, with overdue status highlighting and empty state guidance.

Purpose: Delivers LOAN-03 (overdue flag visible to students) and LOAN-04 (student active loan view).

Output:
- frontend/src/pages/MyLoansPage.tsx — student loans card list
- frontend/src/App.tsx — /my-loans route added
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-loans-circulation/03-CONTEXT.md
@.planning/phases/02-catalog/02-03-SUMMARY.md
@.planning/phases/03-loans-circulation/03-02-SUMMARY.md
@frontend/src/components/BookSearchCard.tsx
@frontend/src/App.tsx
@frontend/src/api/loans.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MyLoansPage student card list and App.tsx /my-loans route</name>
  <read_first>
    - frontend/src/components/BookSearchCard.tsx — card visual style, Card + CardContent from shadcn, layout patterns to reference for student loan card style
    - frontend/src/pages/CatalogPage.tsx — empty state pattern, "No books found" message style, Link usage
    - frontend/src/App.tsx — current state after Plan 02 edits (has /librarian/loans route, needs /my-loans)
    - frontend/src/api/loans.ts — add getMyLoans() function if not already present (check if it was added in Plan 01/02; if not, add: getMyLoans(): fetches GET /api/loans/my, returns { items: LoanItem[], total: number })
    - frontend/src/store/auth.ts — useAuthStore for user info if needed
  </read_first>
  <action>
    Create frontend/src/pages/MyLoansPage.tsx (per D-10, D-11, D-12, D-13):

    Route: /my-loans

    Data fetching:
    - useQuery key: ['my-loans'], query function: getMyLoans() (from frontend/src/api/loans.ts)
    - Loading state: show 2-3 skeleton card placeholders (div with animate-pulse, same height as a card)
    - Error state: "Failed to load your loans." text

    Layout:
    - Outer wrapper: max-w-3xl mx-auto px-6 py-8 (narrower than librarian tables — card list doesn't need full width)
    - Heading: "My Loans" h1 className="text-2xl font-bold text-gray-900 mb-6"
    - Card list: flex flex-col gap-4

    Each loan card (per D-11):
    - Use shadcn Card + CardContent (import from "@/components/ui/card")
    - Book title: text-lg font-semibold text-gray-900
    - Book author: text-sm text-gray-500 (from loan.book — note: LoanItem has book.title but not author; if author is not in the API response, show title only and omit author — do NOT invent data; check the LoanItem type in frontend/src/api/loans.ts)
    - Due date: "Due: [formatted date]" — use due_date.toLocaleDateString or new Date(loan.due_date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })
    - Overdue badge: when is_overdue is true, show red inline badge — <span className="inline-flex items-center rounded-full bg-red-100 px-2.5 py-0.5 text-xs font-medium text-red-700">Overdue</span> (per D-11)
    - No return button — students cannot return books (per D-08)
    - No copy barcode — not meaningful to students (per D-11)

    Empty state (per D-13):
    - When items.length === 0 and not loading: show "No active loans. Browse the catalog to find a book." with a Link to="/catalog" className="text-blue-600 hover:underline"

    If getMyLoans is not already exported from frontend/src/api/loans.ts, add it:
    - getMyLoans(): fetches GET /api/loans/my (authenticated via the same auth header pattern as other API calls). Returns LoansListResponse shape.

    Update frontend/src/App.tsx:
    - Import MyLoansPage from "./pages/MyLoansPage"
    - Add route inside the general authenticated AppLayout block (not requireLibrarian): <Route path="/my-loans" element={<MyLoansPage />} />
    - Place it after the existing /catalog route inside the AppLayout (no requireLibrarian) block
    - Note: since AppLayout without requireLibrarian allows both students and librarians, a librarian navigating to /my-loans will see their own loans (which should be empty — librarians are not checked out books in v1). This is acceptable behavior.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5 && echo "Build check done"</automated>
  </verify>
  <done>
    frontend/src/pages/MyLoansPage.tsx exists. Renders a card list of active loans from GET /api/loans/my. Each card shows book title, due date, and red Overdue badge when is_overdue=true. Empty state shows message with link to /catalog. App.tsx has /my-loans route in the authenticated layout block. TypeScript compiles clean and npm run build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 circulation workflow:
    - Loan model, migration 0005, and backend API (Plan 01)
    - Librarian Loans Dashboard with Active/Overdue tabs and Return action (Plan 02)
    - Checkout modal on Book Detail page with student search (Plan 02)
    - Student My Loans page with overdue badge and empty state (this plan)
    - NavBar extended with "My Loans" (student) and "Loans" (librarian) links
  </what-built>
  <how-to-verify>
    Start the Docker Compose stack: `docker compose up`

    **1. Checkout flow:**
    - Log in as librarian
    - Navigate to /librarian/books → click a book → go to Book Detail
    - Find a copy with status "available" — verify "Check Out" button appears
    - Click "Check Out" → verify modal opens with student search input
    - Type part of a student email → verify matching students appear in dropdown
    - Select a student → verify their name/email shows as selected and due date is displayed (read-only)
    - Click "Confirm Checkout" → verify modal closes, copy status changes from "Available" to "On Loan"

    **2. Librarian Loans Dashboard:**
    - Navigate to /librarian/loans (Loans link in nav)
    - Verify "Active Loans" tab is selected by default
    - Verify the checked-out loan appears with correct columns: student, book title, copy, due date, Active status badge, Return button
    - Click "Overdue Loans" tab → verify only past-due loans appear (may be empty if no overdue loans exist)

    **3. Return flow:**
    - In Active Loans tab, click Return button on the loan just created
    - Verify ConfirmDialog appears asking to confirm return
    - Click confirm → verify row disappears immediately from the table
    - Navigate back to Book Detail → verify copy status is back to "Available"

    **4. Student My Loans:**
    - Log out, log in as the student used for checkout
    - Navigate to /my-loans (My Loans link in nav)
    - Verify the active loan card appears with book title, due date, and no Overdue badge (loan is current)
    - Verify the loan disappears after it was returned (refresh if needed)
    - Verify empty state shows "No active loans. Browse the catalog to find a book."

    **5. Overdue detection:**
    - Manually update a loan's due_date in the DB to yesterday to test overdue:
      `docker compose exec db psql -U postgres -d librarydb -c "UPDATE loans SET due_date = NOW() - INTERVAL '1 day' WHERE returned_at IS NULL LIMIT 1;"`
    - Reload /my-loans → verify red "Overdue" badge appears on that loan
    - Reload /librarian/loans "Overdue Loans" tab → verify that loan appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→GET /api/loans/my | Authenticated student; user_id comes from JWT sub, never from client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Info disclosure | GET /api/loans/my | mitigate | user_id sourced exclusively from get_current_user JWT dependency — student cannot see another student's loans by changing a URL param |
| T-03-11 | Elevation of privilege | /my-loans route access | accept | AppLayout allows both roles; librarian seeing their own (empty) loans is harmless; RBAC enforced server-side |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | no new npm packages — Card from shadcn/ui already installed; no install tasks |
</threat_model>

<verification>
Run after all tasks complete:

1. TypeScript type check:
   ```
   cd frontend && npx tsc --noEmit
   ```

2. Production build:
   ```
   cd frontend && npm run build
   ```

3. MyLoansPage exists:
   ```
   ls frontend/src/pages/MyLoansPage.tsx
   ```

4. App.tsx has /my-loans route:
   ```
   grep "my-loans\|MyLoansPage" frontend/src/App.tsx
   ```

5. Overdue badge in MyLoansPage:
   ```
   grep -i "overdue\|is_overdue\|red" frontend/src/pages/MyLoansPage.tsx
   ```

6. Empty state link to catalog:
   ```
   grep "/catalog" frontend/src/pages/MyLoansPage.tsx
   ```
</verification>

<success_criteria>
- frontend/src/pages/MyLoansPage.tsx exists and renders active loans as cards (per D-12)
- Each card shows book title, due date, and red Overdue badge when is_overdue=true (per D-11)
- No return button on student cards (per D-08)
- Empty state: "No active loans. Browse the catalog to find a book." with /catalog link (per D-13)
- App.tsx has /my-loans route in authenticated (non-requireLibrarian) AppLayout block
- TypeScript compiles clean and npm run build passes
- Human verification: complete checkout → return → overdue detection cycle passes in the running stack
</success_criteria>

<output>
Create .planning/phases/03-loans-circulation/03-03-SUMMARY.md when done.
</output>
