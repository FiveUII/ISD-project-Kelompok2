---
context: phase
phase: 01-foundation-auth
task: 0
total_tasks: 8
status: ready_to_execute
last_updated: 2026-06-08T12:18:54.277Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session — planning passed verification with 0 issues._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Book/Copy conflation | Storing availability as a count on the Book row makes multi-copy tracking impossible without a destructive migration | blocking | Plan 01-01 Task 1 explicitly creates separate book.py and copy.py models; acceptance criterion grep-asserts no `available`/`quantity` fields on Book |
| JWT secret committed to git | JWT_SECRET_KEY hardcoded instead of env var causes credential exposure | advisory | Plan 01-03 references Settings class reading from environment; .env.example must not contain real secrets |

<current_state>
Phase 1 planning is COMPLETE. All 3 plans created and verified (0 blockers, 0 warnings).

Project is a fresh greenfield Library Management System (FastAPI + React + PostgreSQL + Docker).
We are at the start of Phase 1 execution — no code has been written yet.

Plans in .planning/phases/01-foundation-auth/:
- 01-01-PLAN.md — Walking Skeleton (Wave 1)
- 01-02-PLAN.md — Auth slice: register/verify/login (Wave 2, depends on 01-01)
- 01-03-PLAN.md — RBAC + admin seed + password reset (Wave 3, depends on 01-02)
- SKELETON.md — Walking Skeleton architectural decisions
</current_state>

<completed_work>
This session (2026-06-08):
- Project initialized (/gsd-new-project): PROJECT.md, config.json, research (4 agents), REQUIREMENTS.md, ROADMAP.md, STATE.md, CLAUDE.md
- Phase 1 discussion (/gsd-discuss-phase 1): 01-CONTEXT.md, 01-DISCUSSION-LOG.md
- Phase 1 planning (/gsd-plan-phase 1 --skip-ui): 01-01-PLAN.md, 01-02-PLAN.md, 01-03-PLAN.md, SKELETON.md
- All artifacts committed to main branch (latest: 89616a6)
</completed_work>

<remaining_work>
- Execute Phase 1: /gsd-execute-phase 1
  - Wave 1: 01-01 (Walking Skeleton — Docker + data model + DB round-trip)
  - Wave 2: 01-02 (Auth — register, email verify, JWT login)
  - Wave 3: 01-03 (RBAC + admin seed + role promotion + password reset)
- Then: /gsd-discuss-phase 2, /gsd-plan-phase 2, /gsd-execute-phase 2 (Catalog)
- Then: Phases 3 and 4
</remaining_work>

<decisions_made>
- Students self-register at /auth/register; librarians use same flow, promoted by admin superuser
- Admin superuser seeded via ADMIN_EMAIL/ADMIN_PASSWORD env vars at startup (no-op if exists)
- JWT in localStorage, 24h TTL, no refresh token in v1
- Librarian promotion via /admin/users/{id}/promote API only (no UI)
- No account deactivation in v1
- SQLAlchemy 2.0 async (NOT SQLModel — async support lags)
- Book/Copy schema separation is non-negotiable (cannot retrofit after loan data)
- RBAC via FastAPI DI at router level (not per-route, not middleware)
- Docker: base docker-compose.yml + docker-compose.override.yml for dev hot reload
- Hot reload: FastAPI (uvicorn --reload + volume mount) + React (Vite HMR)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-foundation-auth/01-CONTEXT.md` — all implementation decisions for Phase 1
2. `.planning/phases/01-foundation-auth/SKELETON.md` — Walking Skeleton architectural decisions
3. `.planning/phases/01-foundation-auth/01-01-PLAN.md` — Wave 1 plan (start here)
4. `.planning/research/PITFALLS.md` — critical pitfalls (Book/Copy, auth gaps, Docker readiness)
5. `.planning/ROADMAP.md` — overall phase structure and success criteria

## Infrastructure State
- No running services — project not yet started
- No code written — fully greenfield

<context>
Full project initialization completed in one session. Research agents produced high-quality findings on stack, features, architecture, and pitfalls. Phase 1 plans are tight 3-wave vertical slices (MVP mode with Walking Skeleton). Planner used opus model and produced well-structured plans with threat models, concrete acceptance criteria, and pitfall mitigations baked in.

The plan checker verified all 4 auth requirements covered with 0 issues. Two informational decisions (D-02, D-04) were tagged but the gsd-tools coverage check didn't recognize the [informational] tag — this is a cosmetic issue, not a real gap. Both decisions are scope-exclusions with no implementation action.
</context>

<next_action>
Start with: /clear then /gsd-execute-phase 1
</next_action>
